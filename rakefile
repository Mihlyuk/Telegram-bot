require 'redis'
require 'curl'
require 'json'
require 'cgi'

db = Redis.new
curl = CURL.new

desc "This task is called by the Heroku scheduler add-on"
task :reminder do
  db = Redis.new
  puts db.ping
end
